# 割り勘計算Webアプリケーション 要件定義

## 1. プロジェクト概要

グループで遊んでいる際に、誰かが一時的に立替払いをし、最後にまとめて精算するシーンを支援するWebアプリ。
1店舗だけでなく、複数の支払い（飲み会→2次会→タクシーなど）を1つのセッションとして記録・管理し、
最終的に「誰が誰にいくら渡せばよいか」を最小の送金回数で算出する。

スマートフォンからの利用を主とし、シンプルな操作で素早く記録・シェアできることを目指す。

---

## 2. 利用シーンと基本的なデータ構造

### 2.1 利用フロー

1. セッション（例：「渋谷飲み会」）を作成し、メンバーを登録する
2. 支払いが発生するたびに「誰が」「何に対して」「いくら」払ったかを記録する
3. 支払い記録を追加するたびにリアルタイムで精算結果が更新される
4. 最終的に「誰が誰にいくら渡せばOK」を確認してセッションを終了する

### 2.2 データ構造イメージ

```
セッション
├── セッション名（例：渋谷飲み会）
├── メンバー一覧（名前）
├── 通貨設定
├── 端数処理設定
└── 支払い記録[]
    ├── 支払い名（例：1次会、タクシー代）
    ├── 立替者（メンバー1人）
    ├── 合計金額
    ├── 割り勘対象メンバー（セッションメンバーから個別に除外可能）
    └── 個別金額指定（特定メンバーの負担額を固定、残額を他メンバーで均等割り）
```

---

## 3. 機能要件

### 3.1 セッション管理
- セッション（グループ・イベント）単位で複数の支払いをまとめて管理する
- セッション名を入力して作成する
- セッションはLocalStorageに保存され、履歴として閲覧・再開できる

### 3.2 メンバー管理
- セッション内でメンバーの**名前を入力して管理**する
- メンバーはセッション作成後も**後から追加**できる

### 3.3 支払い記録
- 1セッション内に支払い記録を複数追加できる
- 各支払い記録に以下を入力する
  - 支払い名（例：1次会、カラオケ、タクシー代）
  - 立替者（セッションメンバーから1人選択）
  - 合計金額
- 支払い記録を追加・編集するたびに精算結果がリアルタイム更新される

### 3.4 割り勘対象の設定
- 各支払いの割り勘対象は、デフォルトでセッション全メンバー
- 支払いごとに特定メンバーを**対象から除外**できる（例：2次会は参加者のみ）

### 3.5 計算方式
- **均等割り**（全員同額）と**固定金額指定式**の両方に対応する
- 合計金額のみ入力すれば均等割りで計算できる
- 参加者ごとに**固定金額を個別に指定**できる（例：Aさんは3,000円固定）
  - 固定金額を指定した参加者の残額を、残りの参加者で均等割りする

### 3.6 端数処理
- 1人あたりの金額に端数が出た場合の処理方式を**セッション設定で選択**できる
  - 切り捨て
  - 切り上げ
  - 四捨五入

### 3.7 差額調整
- 端数処理後の誤差（合計との差額）は、**結果画面でユーザーが手動で負担者を指定**する
- ※将来的に自動割り当て方式へ変更できるよう、ロジックを独立モジュールとして実装すること

### 3.8 精算結果の表示
- セッション内の全支払いを集計し、**「誰が誰にいくら渡せばOK」を最小の送金回数**で表示する
- 支払い記録の追加・変更のたびにリアルタイムで更新する

### 3.9 シェア機能
- セッションの状態を**URLでシェア**できる
  - URLにアクセスすると同じセッション内容・精算結果が再現できる（Base64エンコード等でクエリパラメータに状態を保持）

### 3.10 履歴管理
- 過去のセッションを**ブラウザのLocalStorageに保存・閲覧**できる
- ログイン不要

### 3.11 通貨
- **複数通貨に対応**する（セッション単位で通貨を選択、各通貨の記号・フォーマットを適用）

---

## 4. 技術要件

| 項目 | 内容 |
|------|------|
| フレームワーク | React |
| 言語 | JavaScript |
| スタイリング | Tailwind CSS（推奨） |
| ビルドツール | Vite |
| バックエンド | なし（フロントエンドのみ） |
| データ永続化 | LocalStorage |
| URLシェア | Base64エンコード等でクエリパラメータに状態を保持 |
| デプロイ先 | GitHub Pages |

---

## 5. UI/UX要件

### 5.1 デバイス対応
- **スマートフォン優先（モバイルファースト）**設計
- PC・タブレットでも問題なく表示されること

### 5.2 テーマ
- ユーザーが以下の**2種類のテーマを切り替え**できる
  - シンプル・ミニマル
  - カラフル・ポップ
- さらに**ライト / ダークモードの切り替え**にも対応する
- テーマ選択はLocalStorageに保存し、次回アクセス時も維持する

### 5.3 画面構成（マルチステップ形式）

```
[ホーム画面]
  ├── 新規セッション作成
  └── 過去のセッション履歴一覧

[セッション画面]（メイン画面）
  ├── セッション名・メンバー表示
  ├── 支払い記録一覧（追加・編集・削除）
  ├── 精算結果サマリー（リアルタイム更新）
  └── URLシェアボタン

[支払い記録 追加/編集画面]
  ├── 支払い名入力
  ├── 立替者選択
  ├── 合計金額入力
  ├── 割り勘対象メンバー選択（除外設定）
  └── 個別金額指定（任意）

[精算結果詳細画面]
  ├── 「誰が誰にいくら」最小送金リスト
  └── 差額調整（手動で負担者指定）
```

### 5.4 多言語対応
- **i18n対応**（国際化）を実装する
- 最低対応言語：日本語・英語
- 言語切り替えUIを設ける

---

## 6. 非機能要件

- LocalStorageに保存するセッションデータはJSON形式とし、スキーマを明確に定義すること
- URLシェア用のクエリパラメータはBase64エンコードで扱いやすい形式にすること
- 精算計算（最小送金回数アルゴリズム）・差額調整ロジックは独立したモジュールとして実装し、後から変更しやすい構造にすること
- テーマ・ダークモード切り替えはCSSカスタムプロパティ（CSS変数）またはTailwindのdarkクラスで管理すること

---

## 7. 未決事項・将来検討事項

| 項目 | 現状 | 将来検討 |
|------|------|---------|
| 差額調整の自動割り当て | 手動でユーザー指定 | 幹事自動負担・最少人数に集中など複数方式を選択式にする可能性あり |

---

## 8. ディレクトリ構成（推奨）

```
src/
├── components/
│   ├── screens/           # 各画面コンポーネント
│   │   ├── HomeScreen.jsx
│   │   ├── SessionScreen.jsx
│   │   ├── PaymentFormScreen.jsx
│   │   └── SettlementScreen.jsx
│   ├── ui/                # 汎用UIパーツ
│   └── layout/            # レイアウト
├── hooks/                 # カスタムフック
├── lib/
│   ├── calculator.js      # 割り勘計算・最小送金アルゴリズム
│   ├── adjustment.js      # 差額調整ロジック（独立モジュール）
│   ├── storage.js         # LocalStorage操作
│   └── share.js           # URLシェア用エンコード/デコード
├── i18n/                  # 多言語リソース
│   ├── ja.json
│   └── en.json
├── styles/                # グローバルスタイル・テーマ定義
└── App.jsx
```
