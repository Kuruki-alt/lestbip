/**
 * adjustment.js — 差額調整ロジック（独立モジュール / 要件 §3.7 / §7）
 *
 * 本ファイルはワイヤーフレーム段階の雛形です。実装はスコープ外。
 *
 * 将来の責務:
 *  - 端数処理後に発生した合計との差額を算出
 *  - 現状: ユーザーが手動で負担者を指定（手動方式）
 *  - 将来: 幹事自動負担・最少人数集中などの方式を切替可能にする
 *
 * @returns {never} 未実装
 */
export function applyManualAdjustment() {
  // TODO(wireframe): 差額調整ロジックはワイヤーフレームのスコープ外
  throw new Error('adjustment.applyManualAdjustment is not implemented yet');
}
