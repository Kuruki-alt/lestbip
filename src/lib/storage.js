/**
 * storage.js — LocalStorage 永続化（要件 §3.1 / §3.10 / §6）
 *
 * 本ファイルはワイヤーフレーム段階の雛形です。永続化はスコープ外。
 *
 * 将来の責務:
 *  - セッションデータ（JSON スキーマ）を LocalStorage へ保存/読込
 *  - セッション履歴の列挙・削除
 *
 * @returns {never} 未実装
 */
export function loadSessions() {
  // TODO(wireframe): LocalStorage 永続化はワイヤーフレームのスコープ外
  throw new Error('storage.loadSessions is not implemented yet');
}

/**
 * @returns {never} 未実装
 */
export function saveSession() {
  // TODO(wireframe): LocalStorage 永続化はワイヤーフレームのスコープ外
  throw new Error('storage.saveSession is not implemented yet');
}
